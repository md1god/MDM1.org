name: Deploy
on:
  push: { branches: [main] }
  workflow_dispatch:
permissions: { contents: read, pages: write, id-token: write }
jobs:
  deploy:
    uses: md1god/MD-automation/.github/workflows/reusable-deploy-static.yml@main
    with: { site-dir: '.' }
