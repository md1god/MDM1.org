name: Deploy
on:
  push: { branches: [main] }
  workflow_dispatch:
permissions: { contents: read, pages: write, id-token: write }
jobs:
  deploy:
    uses: md1god/MD-automation/.github/workflows/reusable-deploy-static.yml@2345badbc0e24b45bdc310e0bbc8d8aca3cb0331
    with: { site-dir: '.' }
