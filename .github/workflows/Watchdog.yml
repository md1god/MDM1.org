name: Watchdog
on:
  schedule: [{ cron: '0 * * * *' }]
  workflow_dispatch:
jobs:
  check:
    permissions: { contents: read, issues: write, actions: read }
    uses: md1god/MD-automation/.github/workflows/reusable-watchdog.yml@2345badbc0e24b45bdc310e0bbc8d8aca3cb0331
    with: { build-workflow-file: 'Deploy.yml' }
