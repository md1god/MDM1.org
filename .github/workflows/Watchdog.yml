name: Watchdog
on:
  schedule: [{ cron: '0 * * * *' }]
  workflow_dispatch:
jobs:
  check:
    permissions: { contents: read, issues: write, actions: read }
    uses: md1god/MD-automation/.github/workflows/reusable-watchdog.yml@main
    with: { build-workflow-file: 'Deploy.yml' }
