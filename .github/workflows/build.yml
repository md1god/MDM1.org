name: Build
on:
  push: { branches: [main] }
  pull_request: { branches: [main] }
  workflow_dispatch: {}
jobs:
  build:
    uses: md1god/MD-robot/.github/workflows/reusable-build.yml@main
    with: { build-command: npm run build }
