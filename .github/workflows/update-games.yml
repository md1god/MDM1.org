name: Fetch All Games Catalog Daily

on:
  schedule:
    - cron: '0 2 * * *' # يعمل يومياً الساعة 2 صباحاً
  workflow_dispatch: # إمكانية التشغيل اليدوي بنقرة زر

# إضافة صلاحيات الكتابة ليتمكن الـ Bot من الحفظ والرفع إلى المستودع
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch Games
        run: node scripts/fetch-games.js

      - name: Commit and Push Updated Catalog
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data/games.json data/games-meta.json
          git commit -m "Auto-update full games catalog [skip ci]" || exit 0
          git push
