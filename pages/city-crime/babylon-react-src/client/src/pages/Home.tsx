// Concrete Meridian: صفحة اللعبة تترك المشهد ثلاثي الأبعاد يتصدر الشاشة، وReact يوفّر الإطار فقط.
import GameCanvas from "@/components/GameCanvas";

export default function Home() {
  return <GameCanvas />;
}
